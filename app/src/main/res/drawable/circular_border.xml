<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" /> <!-- Transparent center -->
    <stroke
        android:width="1dp"
    android:color="@color/green" />  <!-- Set the color of the border to green -->
</shape>